export class Temporizador {
    constructor(title, hours, minutes, seconds, id) {
        this.title = title,
        this.hours = hours,
        this.minutes = minutes,
        this.seconds = seconds,
        this.id = id
    }
}